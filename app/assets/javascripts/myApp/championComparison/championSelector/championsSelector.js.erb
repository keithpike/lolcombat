/**
* ChampionSelector Module
*
* Description
*/
angular.module('ChampionSelector', [
	//'models.champions'
])

	.config(function($stateProvider){
		$stateProvider
			.state('appStart.home.champions', {
				url: '',
				views: {
					'championSelector': {
						templateUrl: "<%= asset_path('./championSelector.html') %>",
						controller: 'ChampionsSelectorController',
						resolve: {
				 		}
					}
				}
			});
	})

	.controller('ChampionsSelectorController', function(champions){
		debugger
		var championSelectorController = this;
	});