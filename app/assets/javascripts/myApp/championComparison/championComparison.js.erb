/**
* championComparison Module
*
* Description
*/
angular.module('championComparison', [
	'itemSelector',
	//'championCalculator',
	//'championSelector',
	//'itemRuneMasterySelector',
	'models.items',
	'models.champions'
])
	.config(function($stateProvider){
		$stateProvider
			.state('appStart.home', {
				url: '/',

				 views: {
				 	'':{
				 		templateUrl: "<%= asset_path('myApp/championComparison/championComparison.html') %>",
				 		controller: 'ChampionComparisonController',
				 		resolve: {
				 			items: function(ItemsModel){
				 				return ItemsModel.getItems();
				 			}
				 		}
				 	},
				 	'navbar': {
				 		templateUrl: "<%= asset_path('myApp/navbar/navbar.html') %>",
				 	},
					'footer': {
						templateUrl: "<%= asset_path('myApp/footer/footer.html') %>",
					}
				 }
			});
	})

	.controller('ChampionComparisonController', function championComparisonController(){
		var championComparisonController = this;
	})