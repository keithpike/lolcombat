/**
* championComparison Module
*
* Description
*/
angular.module('championComparison', ['championCalculator'])
	.config(['$stateProvider', function($stateProvider){
		$stateProvider
			.state('appStart.comparison', {
				abstract: true,
				url: '/calculator',
				views: {
				 	'': {
				 		templateUrl: "<%= asset_path('myApp/championComparison/championComparison.html') %>",
				 	}
				}
			});
	}])